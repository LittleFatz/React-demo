apiVersion: v1
kind: Service
metadata:
  name: fe-demo-service
  labels:
    app: fe-demo
spec:
  type: ClusterIP  # 可选: ClusterIP, NodePort, LoadBalancer
  selector:
    app: fe-demo
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  sessionAffinity: None
