# Docker Build 概念解析

## 核心问题：镜像保存在哪里？

### ✅ 简单答案

**是的！** 当你执行 `docker build` 成功后，镜像会**自动保存**在本地 Docker 中，你可以立即在 Docker Desktop 的 Images 页面看到它。

## 工作流程

```
┌─────────────────────────────────────────────────────────┐
│  第 1 步: 执行构建命令                                   │
├─────────────────────────────────────────────────────────┤
│  $ docker build -t fe-demo:latest .                     │
│                                                          │
│  -t fe-demo:latest  ← 给镜像命名和打标签                 │
│  .                  ← 构建上下文（当前目录）              │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│  第 2 步: Docker 构建镜像                                │
├─────────────────────────────────────────────────────────┤
│  - 读取 Dockerfile                                       │
│  - 下载基础镜像（如果本地没有）                           │
│  - 逐层构建                                              │
│  - 创建最终镜像                                          │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│  第 3 步: 自动保存到本地 Docker                          │
├─────────────────────────────────────────────────────────┤
│  ✅ 镜像保存在本地存储                                   │
│  ✅ Docker Engine 管理                                   │
│  ✅ 无需手动保存或推送                                   │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│  第 4 步: 可以立即使用                                   │
├─────────────────────────────────────────────────────────┤
│  ✓ docker images              ← 命令行查看               │
│  ✓ Docker Desktop → Images    ← GUI 查看                │
│  ✓ docker run fe-demo:latest  ← 直接运行                │
└─────────────────────────────────────────────────────────┘
```

## 详细说明

### Docker 本地存储

Docker 维护一个本地的镜像仓库（Image Repository），所有构建或拉取的镜像都保存在这里。

```
你的电脑
├── Docker Desktop
│   └── Docker Engine
│       └── 本地镜像仓库
│           ├── node:18-alpine          ← 构建时下载的
│           ├── nginx:alpine            ← 构建时下载的
│           ├── fe-demo:latest          ← 你构建的 ✨
│           ├── redis:latest
│           └── ...
```

### 镜像存储位置（物理路径）

虽然你通常不需要直接访问，但镜像实际保存在：

**macOS:**
```
~/Library/Containers/com.docker.docker/Data/vms/0/
```

**Windows:**
```
C:\ProgramData\DockerDesktop\vm-data\
```

**Linux:**
```
/var/lib/docker/overlay2/
```

### 镜像 ID vs 镜像标签

```bash
$ docker images fe-demo

REPOSITORY   TAG       IMAGE ID       CREATED          SIZE
fe-demo      latest    9d5a2a3084b3   10 minutes ago   53.6MB
             ↑         ↑              ↑                ↑
           标签      镜像ID         创建时间         大小
```

- **镜像 ID**: 唯一标识符（SHA256 哈希值）
- **标签**: 人类可读的名称（`repository:tag`）

一个镜像可以有多个标签：
```bash
docker build -t fe-demo:latest -t fe-demo:v1.0.0 -t fe-demo:prod .

# 结果：一个镜像，三个标签
fe-demo:latest     9d5a2a3084b3   53.6MB
fe-demo:v1.0.0     9d5a2a3084b3   53.6MB  ← 同一个镜像ID
fe-demo:prod       9d5a2a3084b3   53.6MB
```

## 常见场景对比

### 场景 1: 只构建，不推送（你的情况）

```bash
# 构建镜像
$ docker build -t fe-demo:latest .

# ✅ 镜像在本地
$ docker images fe-demo
REPOSITORY   TAG       SIZE
fe-demo      latest    53.6MB

# ✅ 可以直接运行
$ docker run -d -p 8080:80 fe-demo:latest

# ❌ 其他电脑看不到（还没推送）
```

**用途**: 本地开发、测试

### 场景 2: 构建并推送到远程

```bash
# 1. 构建镜像
$ docker build -t myusername/fe-demo:latest .

# 2. 推送到 Docker Hub
$ docker push myusername/fe-demo:latest

# 现在的状态：
# ✅ 本地有镜像
# ✅ Docker Hub 有镜像
# ✅ 其他人可以 pull
```

**用途**: 分享、生产部署

### 场景 3: 从远程拉取

```bash
# 拉取镜像
$ docker pull nginx:alpine

# ✅ 镜像保存到本地
$ docker images nginx
REPOSITORY   TAG       SIZE
nginx        alpine    23MB
```

## 验证镜像是否在本地

### 方法 1: 命令行

```bash
# 查看所有镜像
docker images

# 查看特定镜像
docker images fe-demo

# 查看镜像详细信息
docker inspect fe-demo:latest

# 搜索镜像
docker images | grep fe-demo
```

### 方法 2: Docker Desktop GUI

1. 打开 Docker Desktop
2. 点击左侧 **Images** 标签
3. 看到所有本地镜像列表
4. 可以搜索、排序、删除

### 方法 3: API

```bash
# Docker 提供 REST API
curl --unix-socket /var/run/docker.sock http://localhost/images/json
```

## 镜像的生命周期

```
┌─────────────┐
│   构建/拉取  │
│   ↓          │
│   本地存储   │ ← 你现在在这里
│   ↓          │
│   运行容器   │
│   ↓          │
│   推送远程   │ (可选)
│   ↓          │
│   删除镜像   │
└─────────────┘
```

### 从构建到运行

```bash
# 1. 构建 → 镜像保存在本地
docker build -t myapp:latest .

# 2. 查看 → 确认镜像存在
docker images myapp

# 3. 运行 → 从镜像创建容器
docker run -d -p 8080:80 myapp:latest

# 4. 验证 → 容器运行中
docker ps
```

## 镜像 vs 容器

这是两个不同的概念：

```
┌────────────────────┐           ┌────────────────────┐
│   镜像 (Image)      │           │   容器 (Container)  │
├────────────────────┤           ├────────────────────┤
│                    │           │                    │
│  📦 静态模板        │  创建/运行 │  🏃 运行实例        │
│                    │ ─────────→│                    │
│  💿 类似程序安装包   │           │  🖥️  类似运行的程序  │
│                    │           │                    │
│  📝 只读            │           │  ✏️  可读写          │
│                    │           │                    │
│  docker build 创建 │           │  docker run 创建   │
│  docker images 查看│           │  docker ps 查看    │
│                    │           │                    │
└────────────────────┘           └────────────────────┘

一个镜像可以创建多个容器:

fe-demo:latest (镜像)
    ├── container-1 (端口 8080)
    ├── container-2 (端口 8081)
    └── container-3 (端口 8082)
```

## 常见误区

### ❌ 误区 1: 以为需要"保存"镜像

```bash
# ❌ 错误理解
docker build -t myapp .
docker save ???  # 不需要！已经自动保存了

# ✅ 正确理解
docker build -t myapp .
# 构建完成 = 已保存在本地
```

`docker save` 只是用来导出镜像到文件，不是保存到本地！

### ❌ 误区 2: 以为需要"推送"才能用

```bash
# ❌ 错误理解
docker build -t myapp .
docker push ???  # 本地使用不需要推送

# ✅ 正确理解
docker build -t myapp .
docker run myapp  # 直接用！
```

推送（push）是为了分享给其他人或远程服务器。

### ❌ 误区 3: 混淆镜像和容器

```bash
# ❌ 错误理解
docker build 创建容器  # 错！创建的是镜像

# ✅ 正确理解
docker build  → 创建镜像
docker run    → 从镜像创建容器
```

## 实际操作演示

让我们通过一个完整例子看看镜像如何"出现"：

```bash
# 初始状态：没有 mytest 镜像
$ docker images mytest
REPOSITORY   TAG       IMAGE ID   CREATED   SIZE
# (空的)

# 创建一个简单的 Dockerfile
$ cat > Dockerfile <<EOF
FROM alpine:latest
RUN echo "Hello World" > /hello.txt
EOF

# 构建镜像
$ docker build -t mytest:latest .
[+] Building 2.3s (6/6) FINISHED
...

# 🎉 现在镜像出现了！
$ docker images mytest
REPOSITORY   TAG       IMAGE ID       CREATED          SIZE
mytest       latest    abc123def456   5 seconds ago    7.05MB

# 在 Docker Desktop 中也能看到
# GUI: Images → 搜索 "mytest" → 看到了！

# 可以直接使用
$ docker run --rm mytest cat /hello.txt
Hello World
```

**整个过程不需要任何额外的"保存"或"导入"操作！**

## 总结

### 核心要点

1. ✅ `docker build` 成功 = 镜像自动保存在本地
2. ✅ 可以立即在 Docker Desktop 看到
3. ✅ 可以立即用 `docker run` 运行
4. ✅ 不需要任何额外的保存操作

### 何时需要"推送"

只有在以下情况才需要 `docker push`:

- 🌐 分享给其他人
- ☁️  部署到远程服务器
- 📦 备份到云端
- 👥 团队协作

### 类比理解

```
编程世界               Docker 世界
─────────────         ─────────────
源代码                Dockerfile
  ↓ 编译                ↓ build
可执行文件             镜像 (Image)
  ↓ 运行                ↓ run
进程                  容器 (Container)

编译完 → 文件在磁盘    构建完 → 镜像在 Docker
无需"保存"            无需"保存"
```

## 快速检查清单

构建镜像后，你应该能做到：

```bash
✅ docker images              # 看到你的镜像
✅ docker inspect myapp       # 查看镜像详情
✅ docker run myapp           # 运行容器
✅ docker history myapp       # 查看构建历史
```

如果以上命令都能成功执行，说明镜像已经正确保存在本地了！

---

**最简单的理解**:
`docker build` 成功 = 镜像在本地 = Docker Desktop 能看到 = 可以直接用 ✅
