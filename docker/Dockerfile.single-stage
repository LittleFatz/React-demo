# 单阶段构建示例（不推荐）
FROM node:18-alpine

WORKDIR /app

# 安装 nginx
RUN apk add --no-cache nginx

# 复制文件并构建
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

# 配置 nginx
COPY nginx.conf /etc/nginx/conf.d/default.conf
RUN cp -r /app/dist/* /usr/share/nginx/html/

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]

# 问题：
# 1. 最终镜像包含 Node.js、npm、所有开发依赖（不需要）
# 2. 包含源代码、node_modules（不需要）
# 3. 镜像体积可能达到 500MB-1GB
